<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/background_spirituel">

        <!-- Titre centré -->
        <TextView
            android:id="@+id/titleTextView"
            style="@style/TextTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Titre du Cantique"
            android:gravity="center"
            android:textAlignment="center"
            android:layout_marginBottom="4dp" />

        <!-- Catégorie alignée à gauche sous le titre -->
        <TextView
            android:id="@+id/categoryTextView"
            style="@style/TextSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Catégorie"
            android:gravity="start"
            android:layout_marginBottom="12dp" />

        <!-- Paroles -->
        <TextView
            android:id="@+id/lyricsTextView"
            style="@style/TextLyrics"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Paroles du chant"
            android:layout_marginBottom="16dp" />

        <!-- Auteur aligné à droite après les paroles -->
        <TextView
            android:id="@+id/authorTextView"
            style="@style/TextSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Auteur inconnu"
            android:gravity="end"
            android:textAlignment="viewEnd"
            android:layout_marginBottom="16dp" />

        <!-- Bouton écouter -->
        <Button
            android:id="@+id/playButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Écouter le chant"
            android:layout_marginBottom="8dp" />

        <!-- Bouton partition -->
        <Button
            android:id="@+id/sheetButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Voir la partition" />

    </LinearLayout>
</ScrollView>
